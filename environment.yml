name: my-instance
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - requests
  - jupyter
  - langchain
  - langchain-community
  - python-dotenv
  - pip
  - pip:
      - google-generativeai
prefix: /home/josh/miniconda3/envs/my-instance
